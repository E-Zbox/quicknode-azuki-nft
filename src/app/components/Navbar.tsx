"use client";
// styles
import { Label, MainNavbar } from "../styles/Navbar.styles";

const Navbar = () => {
  return (
    <MainNavbar>
      <Label>AZUKI</Label>
    </MainNavbar>
  );
};

export default Navbar;
